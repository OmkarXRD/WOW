<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="290dp"
    android:layout_height="@dimen/dp_250"
    android:layout_gravity="center"
    android:background="@drawable/bg_play_alert_popup">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/txtmsg"
            android:layout_width="@dimen/dp_180"
            android:layout_height="@dimen/dp_130"
            android:gravity="center"
            android:lineSpacingExtra="3dp"
            android:shadowColor="#020653"
            android:shadowRadius="7"
            android:text="Use 50 heart to \n play this Game"
            android:textColor="#ffffff"
            android:textSize="25dp" />

        <LinearLayout
            android:layout_width="@dimen/dp_270"
            android:layout_height="@dimen/dp_50"
            android:background="@drawable/bg_play_alert_button_bg"
            android:gravity="center">

            <Button
                android:id="@+id/btnCancle"
                android:layout_width="105dp"
                android:layout_height="40dp"
                android:layout_margin="10dp"
                android:background="@drawable/btn_play_no" />

            <Button
                android:id="@+id/btnOk"
                android:layout_width="105dp"
                android:layout_height="40dp"
                android:layout_margin="@dimen/dp_10"
                android:background="@drawable/btn_play_yes" />

        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
